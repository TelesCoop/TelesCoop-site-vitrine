---
import InlineSvg from './InlineSvg.astro';

interface Props {
  title: string;
  description: string;
  src?: string;
}

const { title, description, src } = Astro.props;

// Color mapping for SVG adaptation
const colorMapping = {
  '#FFCD4E': '--palette-yellow',
  '#ffcd4e': '--palette-yellow',
  '#FF5631': '--palette-coral',
  '#ff5631': '--palette-coral',
  '#60E093': '--palette-green',
  '#60e093': '--palette-green',
  '#A8CFEE': '--palette-blue',
  '#a8cfee': '--palette-blue',
  '#1E1C1C': '--palette-border',
  '#1e1c1c': '--palette-border',
  '#000000': '--palette-border',
  '#000': '--palette-border',
};
---

<div class="bg-beige border-2 border-gray-200 rounded-lg p-6 hover:border-gray-400 transition-all">
  {src && (
    <div class="mb-4 w-16 h-16 mx-auto">
      <InlineSvg
        src={src}
        alt={title}
        colors={colorMapping}
        class="w-full h-full object-contain"
      />
    </div>
  )}
  <h3 class="card-title text-center mb-3">{title}</h3>
  <p class="text-black leading-relaxed">{description}</p>
</div>
