---
import Logo from "./Logo.astro";
---

<footer class="bg-black text-white py-12">
  <div class="container mx-auto px-4">
    <div class="grid grid-cols-1 xs:grid-cols-2 md:grid-cols-3 gap-12 mb-8">
      <!-- Logo et description -->
      <div>
        <Logo class="text-white" />
        <p class="text-white/80 text-base md:text-lg">
          Coopérative de développement pour des projets engagés
        </p>
        <!-- Logos partenaires -->
        <div class="pt-4 mb-6">
          <p class="text-sm md:text-base mb-2">Partenaires :</p>
          <div class="flex justify-start items-center gap-8">
            <a href="https://www.urscop.fr/" target="_blank" rel="noopener noreferrer" class="opacity-80 hover:opacity-100 transition-opacity">
              <img src="/img/urscop-logo.png" alt="Logo URSCOP" class="h-10 md:h-12" loading="lazy" decoding="async" />
            </a>
            <a href="https://la.zone/" target="_blank" rel="noopener noreferrer" class="opacity-80 hover:opacity-100 transition-opacity">
              <img src="/img/lazone.svg" alt="Logo La Zone" class="h-10 md:h-12" loading="lazy" decoding="async" />
              La Zone
            </a>
          </div>
        </div>
      </div>

      <!-- Navigation -->
      <div>
        <h3 class="text-lg md:text-xl font-semibold mb-4">Navigation</h3>
        <ul class="space-y-2 text-sm md:text-base">
          <li><a href="/coopérative" class="text-white/80 hover:text-red transition-colors">La coopérative</a></li>
          <li><a href="/projets" class="text-white/80 hover:text-red transition-colors">Nos projets</a></li>
          <li><a href="/blog" class="text-white/80 hover:text-red transition-colors">Blog</a></li>
          <li><a href="mailto:contact@telescoop.fr" class="text-white/80 hover:text-red transition-colors">Nous contacter</a></li>
        </ul>
      </div>

      <!-- Contact et infos -->
      <div>
        <h3 class="text-lg md:text-xl font-semibold mb-4">Contact</h3>
        <ul class="space-y-2 text-white/80 text-sm md:text-base">
          <li>
            <a href="mailto:contact@telescoop.fr" class="hover:text-red transition-colors">
              contact@telescoop.fr
            </a>
          </li>
          <li>
            10 rue de Penthièvre<br/>
            75010 Paris
          </li>
          <li class="mt-4">
            <a href="/mentions-legales" class="hover:text-red transition-colors">
              Mentions légales
            </a>
          </li>
          <li>
            <a href="/politique-de-confidentialite" class="hover:text-red transition-colors">
              Politique de confidentialité
            </a>
          </li>
        </ul>
      </div>
    </div>

    <!-- Bottom bar -->
    <div class="border-t border-white/20 pt-8 flex flex-col gap-3 text-center md:text-left">
      <div class="flex flex-col md:flex-row justify-between items-center gap-3">
        <p class="text-white/60 text-xs md:text-sm">
          © {new Date().getFullYear()} TelesCoop - SCOP
        </p>
        <p class="text-white/60 text-xs md:text-sm">
          Site écoconçu · <span id="page-weight">Calcul en cours...</span>
        </p>
      </div>
      <p class="text-white/60 text-xs md:text-sm text-center md:text-left">
        Illustrations : <a href="https://www.lacooperativedesinternets.fr/" target="_blank" rel="noopener noreferrer" class="hover:text-white transition-colors">La Coopérative des Internets</a>
      </p>
    </div>
  </div>
</footer>

<script>
  // Calculate page weight dynamically
  async function calculatePageWeight() {
    try {
      let totalSize = 0;

      // Calculate HTML size
      const htmlSize = new Blob([document.documentElement.outerHTML]).size;
      totalSize += htmlSize;

      // Get all resources loaded by the page
      const resources = performance.getEntriesByType('resource');

      for (const resource of resources) {
        if ('transferSize' in resource) {
          totalSize += (resource as PerformanceResourceTiming).transferSize;
        }
      }

      // Convert to KB
      const sizeInKB = Math.round(totalSize / 1024);

      // Update the display
      const weightElement = document.getElementById('page-weight');
      if (weightElement) {
        weightElement.textContent = `Poids de la page : ~${sizeInKB}ko`;
      }
    } catch (error) {
      console.error('Error calculating page weight:', error);
      const weightElement = document.getElementById('page-weight');
      if (weightElement) {
        weightElement.textContent = 'Poids de la page : calcul impossible';
      }
    }
  }

  // Wait for page to be fully loaded, then add a small delay to ensure all resources are accounted for
  if (document.readyState === 'complete') {
    setTimeout(calculatePageWeight, 1000);
  } else {
    window.addEventListener('load', () => {
      setTimeout(calculatePageWeight, 1000);
    });
  }
</script>
