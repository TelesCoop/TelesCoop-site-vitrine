---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import ValueCard from '../components/ValueCard.astro';
import { getEntry } from 'astro:content';

const values = await getEntry('data', 'values');
const valuesData = values?.data || [];

const title = 'La coopérative';
const description = 'La coopérative TelesCoop et ses particularités.';
---

<Layout title={title} description={description}>
  <Header slot="header" />

  <div class="container mx-auto px-4 py-16 mt-16">
    <div class="grid grid-cols-1 lg:grid-cols-12 gap-8 items-center mb-16">
      <h1 class="lg:col-span-4 text-4xl md:text-5xl font-light text-accent-orange">
        Une coopérative engagée
      </h1>
      <div class="lg:col-span-8 border-l-4 border-accent-orange pl-6">
        <p class="text-lg text-gray-700 leading-relaxed mb-4">
          <strong class="font-semibold text-black">TelesCoop est une SCOP</strong> (Société Coopérative et Participative)
          fondée avec la conviction que le numérique peut être un levier de transformation positive pour la société.
        </p>
        <p class="text-lg text-gray-700 leading-relaxed mb-4">
          Nous nous sommes donné pour mission d'accompagner les acteurs et actrices du changement dans leurs projets numériques.
          Associations, collectivités territoriales, entreprises de l'économie sociale et solidaire : nous collaborons avec
          une grande diversité d'organisations engagées pour les aider à accroître leur impact.
        </p>
        <p class="text-lg text-gray-700 leading-relaxed">
          Au-delà de notre expertise technique, c'est notre <strong class="font-semibold text-black">organisation coopérative</strong>
          et nos <strong class="font-semibold text-black">valeurs partagées</strong> qui font notre différence.
        </p>
      </div>
    </div>

    <section class="mt-16">
      <h2 class="text-3xl font-light mb-8">Notre fonctionnement</h2>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {valuesData.map((value: any) => (
          <ValueCard
            title={value.name}
            description={value.description}
            src={value.src}
          />
        ))}
      </div>
    </section>
  </div>

  <Footer slot="footer" pageWeight={80} />
</Layout>
